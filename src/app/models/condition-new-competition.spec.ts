import { ConditionNewCompetition } from './condition-new-competition';

describe('ConditionNewCompetition', () => {
  it('should create an instance', () => {
    expect(new ConditionNewCompetition()).toBeTruthy();
  });
});
