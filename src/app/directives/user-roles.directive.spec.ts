import { UserRolesDirective } from './user-roles.directive';

describe('UserRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new UserRolesDirective();
    expect(directive).toBeTruthy();
  });
});
